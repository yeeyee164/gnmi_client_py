debug: false
insecure: true
protocol: "gnmi"

subscriptions:
  interfaces_B:
    mode: "stream"
    encoding: "json_ietf"
    subscription:
      path:
      - '/interfaces/interface[name="mgmt0"]/state/counters'
      - '/interfaces/interface[name="te0/2"]/state'
      mode: "sample"
      sample_interval: 90 # seconds

  interfaces:
    mode: "stream"
    encoding: "json_ietf"
    subscription:
      path:
      - '/interfaces/interface[name="te0/12"]/state/counters'
      - '/interfaces/interface[name="te0/13"]/state/counters'
      mode: "sample"
      sample_interval: 100 # seconds
        
targets:
  10.1.11.101:9339:
    subscriptions:
      - interfaces
    username: "admin"
    password: "Frontier12#"
    update_only: false
    insecure: true

outputs:
  log_file:
    type: file
    file-type: stdout
    format: json